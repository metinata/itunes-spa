const api = {
        endpoints: {
            search: 'https://itunes.apple.com/search',
            lookup: 'https://itunes.apple.com/lookup'
        }
    },
    cacheDuration = 3600;

export {
    api,
    cacheDuration
}